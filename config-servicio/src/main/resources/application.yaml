
server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/lin034/repasaSpringCloudConfig.git
          search-paths: config-data
          clone-on-start: true
  application:
    name: config-servicio